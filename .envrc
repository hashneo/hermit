# Hermit development environment
# Managed by direnv — run `direnv allow` after editing.

# Homebrew binaries (go, node, npm, python3, xcpretty, etc.)
PATH_add /opt/homebrew/bin
PATH_add /opt/homebrew/sbin

# Go-installed binaries (gomobile, air, etc.)
PATH_add "${HOME}/go/bin"
